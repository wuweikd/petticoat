# Petticoat Admin

独立 Web 管理后台（Vite + React + Ant Design）。完整 CMS 骨架；当前已接通品牌目录列表/新建。

## 本地

```bash
# 先启动 server（:3001）
cd admin
npm install
npm run dev
```

打开 http://localhost:5173  
种子管理员：手机号 `10000000000`，验证码任意 4 位数字（如 `0000`）。
